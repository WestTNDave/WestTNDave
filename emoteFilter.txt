var strQScrub = q;
strQScrub = strQScrub.toLowerCase();
if(strQScrub.indexOf("westtndave") >= 0 && u!=q)
{
  "WestTnDave - touser:" + u + " - query:" + q;
}
else 
{
  if(u.indexOf("westtn") >= 0 || u==q ) 
  {
    "";
  }
  else 
  {
    u;
  }
}
